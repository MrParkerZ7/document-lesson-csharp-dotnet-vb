<Project Sdk="Microsoft.NET.Sdk">

  <!-- The system under test. net10.0, nullable and warnings-as-errors come from the repo
       Directory.Build.props; the coverage gate lives with the tests (../CoverageGate.props). -->

</Project>

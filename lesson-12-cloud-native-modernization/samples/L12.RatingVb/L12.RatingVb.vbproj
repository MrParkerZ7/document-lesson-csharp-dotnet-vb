<Project Sdk="Microsoft.NET.Sdk">
  <!-- A VB class library: the C# Lambda, the VB Lambda and the container API all call it.
       net10.0 and Option Strict On come from the repository Directory.Build.props. -->
  <PropertyGroup>
    <RootNamespace>L12.RatingVb</RootNamespace>
  </PropertyGroup>
</Project>

<!-- generated by 0-script/build_lessons.py from META in 0-script/lessons/lesson_12.py — do not hand-edit -->
# Lesson 12 · Cloud-Native .NET on AWS & Modernization

> Ship MotorQuote to AWS Lambda and ECS Fargate from one VB rating library — Annotations, SnapStart, SDK container images, Terraform, OIDC, OpenTelemetry — then triage a .NET Framework estate

📄 **Lesson PDF:** [lesson-12-cloud-native-modernization.pdf](./lesson-12-cloud-native-modernization.pdf)

## You will be able to

- Choose between Lambda (managed dotnet10, SnapStart, Native AOT, Managed Instances), ECS Fargate and EC2 Windows + IIS from a workload's traffic shape, latency budget and legacy constraints
- Write, test and package a .NET Lambda function with Lambda Annotations, register SnapStart hooks, and read the handler string Lambda actually calls
- Publish a multi-architecture, non-root, chiseled container image with dotnet publish and no Dockerfile, and make it shut down inside the ECS stop timeout
- Describe both targets in Terraform, deploy them from GitHub Actions with OIDC, and emit OpenTelemetry traces, metrics and structured logs that reach X-Ray and CloudWatch
- Triage a .NET Framework / IIS estate with the AWS 7 Rs, map each API family to its .NET 10 replacement, and plan an incremental migration with YARP and System.Web adapters

## Maps from your stack

Python Lambda functions tuned for cold start and package size, Spring and NestJS services on ECS Fargate, Terraform estates of 1,000–2,000+ resources, GitHub Actions pipelines, and the Windows (IIS) workloads in an 80+ application migration inventory.

## Samples — every code panel in the PDF is read from these files

| Project | Language | Build / run |
|---|---|---|
| [`L12.EstateTriage`](./samples/L12.EstateTriage/L12.EstateTriage.csproj) | C# | `dotnet build samples/L12.EstateTriage/L12.EstateTriage.csproj` |
| [`L12.QuoteContainer`](./samples/L12.QuoteContainer/L12.QuoteContainer.csproj) | C# | `dotnet build samples/L12.QuoteContainer/L12.QuoteContainer.csproj` |
| [`L12.QuoteContainer.Tests`](./samples/L12.QuoteContainer.Tests/L12.QuoteContainer.Tests.csproj) | C# | `dotnet build samples/L12.QuoteContainer.Tests/L12.QuoteContainer.Tests.csproj` |
| [`L12.QuoteLambda`](./samples/L12.QuoteLambda/L12.QuoteLambda.csproj) | C# | `dotnet build samples/L12.QuoteLambda/L12.QuoteLambda.csproj` |
| [`L12.QuoteLambda.Tests`](./samples/L12.QuoteLambda.Tests/L12.QuoteLambda.Tests.csproj) | C# | `dotnet build samples/L12.QuoteLambda.Tests/L12.QuoteLambda.Tests.csproj` |
| [`L12.QuoteLambdaVb`](./samples/L12.QuoteLambdaVb/L12.QuoteLambdaVb.vbproj) | Visual Basic | `dotnet build samples/L12.QuoteLambdaVb/L12.QuoteLambdaVb.vbproj` |
| [`L12.RatingVb`](./samples/L12.RatingVb/L12.RatingVb.vbproj) | Visual Basic | `dotnet build samples/L12.RatingVb/L12.RatingVb.vbproj` |

## Rebuild

```bash
python 0-script/build_lessons.py --only 12   # this PDF
python 0-script/verify_samples.py --only 12  # build + test + run the samples
```

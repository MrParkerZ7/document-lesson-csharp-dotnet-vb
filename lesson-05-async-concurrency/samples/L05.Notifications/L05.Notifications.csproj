<Project Sdk="Microsoft.NET.Sdk">
  <!-- Notification dispatcher: async streams in, bounded channels through, SMS / Email / LINE out. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>
</Project>

<!-- generated by 0-script/build_lessons.py from META in 0-script/lessons/lesson_09.py — do not hand-edit -->
# Lesson 09 · Data Access with EF Core

> DbContext, LINQ-to-SQL, change tracking, concurrency, migrations and Dapper — EF Core 10 read through JPA/Hibernate, TypeORM and Flyway

📄 **Lesson PDF:** [lesson-09-data-access-efcore.pdf](./lesson-09-data-access-efcore.pdf)

## You will be able to

- Map a MotorQuote model with a DbContext, IEntityTypeConfiguration classes and complex types for Money, and say where it differs from JPA entities and @Embeddable
- Predict the SQL a LINQ query sends — read it with ToQueryString, and spot the IEnumerable, missing-Include and N+1 traps by counting commands
- Explain SaveChanges as a unit of work, wrap several writes in one explicit transaction, and build optimistic concurrency on a database with no rowversion
- Ship schema changes with dotnet ef: committed migrations, a CI gate, SQL scripts or migration bundles
- Choose between tracked EF Core, ExecuteUpdate, Dapper and the AWS SDK, and between SQLite in-memory, a real database and the InMemory provider for tests
- Query a C# DbContext from Visual Basic — and know which EF Core tooling refuses VB projects

## Maps from your stack

JPA/Hibernate entity mapping and the persistence context, Spring Data repositories and JdbcTemplate, TypeORM entities and query builders, Flyway/Liquibase migrations, H2 test databases — and the Metabase-style reporting SQL and DynamoDB access code you have written for insurance platforms.

## Samples — every code panel in the PDF is read from these files

| Project | Language | Build / run |
|---|---|---|
| [`L09.Data`](./samples/L09.Data/L09.Data.csproj) | C# | `dotnet build samples/L09.Data/L09.Data.csproj` |
| [`L09.Data.Tests`](./samples/L09.Data.Tests/L09.Data.Tests.csproj) | C# | `dotnet build samples/L09.Data.Tests/L09.Data.Tests.csproj` |
| [`L09.QueryConsole`](./samples/L09.QueryConsole/L09.QueryConsole.csproj) | C# | `dotnet build samples/L09.QueryConsole/L09.QueryConsole.csproj` |
| [`L09.VbModel`](./samples/L09.VbModel/L09.VbModel.vbproj) | Visual Basic | `dotnet build samples/L09.VbModel/L09.VbModel.vbproj` |
| [`L09.VbReport`](./samples/L09.VbReport/L09.VbReport.vbproj) | Visual Basic | `dotnet build samples/L09.VbReport/L09.VbReport.vbproj` |

## Rebuild

```bash
python 0-script/build_lessons.py --only 9   # this PDF
python 0-script/verify_samples.py --only 9  # build + test + run the samples
```
